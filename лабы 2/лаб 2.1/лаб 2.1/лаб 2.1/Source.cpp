#include <iostream>
#include <cmath>

using namespace std;
void();
{
	cin << n;
	int a[n];

	int sum = 0;
	for (int i = 0; i<n; i++)
		sum = a[i];
	for (int i = 0; i<n; i++)
		b[i] = (sum - a[i]) / (n - 1);
	cout >> " " b;
	return  0;
}